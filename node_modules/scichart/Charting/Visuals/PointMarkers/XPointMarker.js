"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.XPointMarker = void 0;
const BasePointMarker_1 = require("./BasePointMarker");
/**
 * @summary Point-marker type which renders an 'x' at each x-y datapoint location
 * @remarks
 * To apply the XPointMarker to a {@link IRenderableSeries}, use the following code:
 *
 * ```ts
 * const sciChartSurface: SciChartSurface;
 * const wasmContext: TSciChart;
 * sciChartSurface.renderableSeries.add(new FastLineRenderableSeries(wasmContext, {
 *              pointMarker: new XPointMarker(wasmContext, {
 *                  width: 9,
 *                  height: 9,
 *                  fill: "#FF0000",
 *                  stroke: "#0000FF",
 *                  strokeThickness: 1
 *              })
 * }));
 * ```
 */
class XPointMarker extends BasePointMarker_1.BasePointMarker {
    /**
     * Creates an instance of the {@link XPointMarker}
     * @param webAssemblyContext The {@link TSciChart | SciChart 2D WebAssembly Context} containing native methods and
     * access to our WebGL2 Engine and WebAssembly numerical methods
     * @param options Optional parameters of type {@link IPointMarkerOptions} used to configure the point-marker at instantiation time
     */
    constructor(webAssemblyContext, options) {
        super(webAssemblyContext, options);
    }
    /**
     * @inheritDoc
     */
    drawSprite(canvas, spriteWidth, spriteHeight, stroke, strokeThickness, fill) {
        // WPF has this
        // var strokeThickness = Stroke.IsTransparent() ? 0d : StrokeThickness.RoundOff();
        strokeThickness = Math.round(strokeThickness) / 2;
        if (strokeThickness > 0) {
            canvas.strokeStyle = stroke;
            canvas.lineWidth = strokeThickness;
            canvas.beginPath();
            canvas.moveTo(strokeThickness, strokeThickness);
            canvas.lineTo(this.width - strokeThickness, this.height - strokeThickness);
            canvas.stroke();
            canvas.beginPath();
            canvas.moveTo(this.width - strokeThickness, strokeThickness);
            canvas.lineTo(strokeThickness, this.height - strokeThickness);
            canvas.stroke();
        }
    }
}
exports.XPointMarker = XPointMarker;
