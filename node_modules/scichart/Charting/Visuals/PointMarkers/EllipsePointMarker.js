"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.EllipsePointMarker = void 0;
const BasePointMarker_1 = require("./BasePointMarker");
/**
 * @summary Point-marker type which renders an ellipse at each x-y datapoint location
 * @remarks
 * To apply the EllipsePointMarker to a {@link IRenderableSeries}, use the following code:
 *
 * ```ts
 * const sciChartSurface: SciChartSurface;
 * const wasmContext: TSciChart;
 * sciChartSurface.renderableSeries.add(new FastLineRenderableSeries(wasmContext, {
 *              pointMarker: new EllipsePointMarker(wasmContext, {
 *                  width: 9,
 *                  height: 9,
 *                  fill: "#FF0000",
 *                  stroke: "#0000FF",
 *                  strokeThickness: 1
 *              })
 * }));
 * ```
 */
class EllipsePointMarker extends BasePointMarker_1.BasePointMarker {
    /**
     * Creates an instance of the {@link EllipsePointMarker}
     * @param webAssemblyContext The {@link TSciChart | SciChart 2D WebAssembly Context} containing native methods and
     * access to our WebGL2 Engine and WebAssembly numerical methods
     * @param options Optional parameters of type {@link IPointMarkerOptions} used to configure the point-marker at instantiation time
     */
    constructor(webAssemblyContext, options) {
        super(webAssemblyContext, options);
    }
    /**
     * @inheritDoc
     */
    drawSprite(canvas, spriteWidth, spriteHeight, stroke, strokeThickness, fill) {
        const centerX = spriteWidth / 2;
        const centerY = spriteHeight / 2;
        canvas.fillStyle = fill;
        canvas.ellipse(centerX, centerY, this.width / 2, this.height / 2, 0, 0, 2 * Math.PI, false);
        canvas.fill();
        if (strokeThickness > 0) {
            canvas.strokeStyle = stroke;
            canvas.lineWidth = strokeThickness;
            canvas.stroke();
        }
    }
}
exports.EllipsePointMarker = EllipsePointMarker;
