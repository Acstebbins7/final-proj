"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ScatterSeriesHitTestProvider = void 0;
const BaseHitTestProvider_1 = require("./BaseHitTestProvider");
/**
 * Hit-test provider for {@link XyScatterRenderableSeries}. See base class {@link BaseHitTestProvider} for further info
 */
class ScatterSeriesHitTestProvider extends BaseHitTestProvider_1.BaseHitTestProvider {
    /**
     * @inheritDoc
     */
    hitTest(point, logic, hitTestRadius, interpolate) {
        if (!this.currentRenderPassData || !point) {
            return undefined;
        }
        let radius = ScatterSeriesHitTestProvider.DEFAULT_HIT_TEST_RADIUS;
        const pointMarker = this.parentSeries.pointMarker;
        if (pointMarker) {
            radius = Math.max(Math.max(pointMarker.width, pointMarker.height), radius);
        }
        return super.hitTest(point, logic, radius, interpolate);
    }
}
exports.ScatterSeriesHitTestProvider = ScatterSeriesHitTestProvider;
