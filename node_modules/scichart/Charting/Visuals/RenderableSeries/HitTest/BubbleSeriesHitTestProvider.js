"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.BubbleSeriesHitTestProvider = void 0;
const BaseHitTestProvider_1 = require("./BaseHitTestProvider");
/**
 * Hit-test provider for {@link FastBubbleRenderableSeries}. See base class {@link BaseHitTestProvider} for further info
 */
class BubbleSeriesHitTestProvider extends BaseHitTestProvider_1.BaseHitTestProvider {
    /**
     * @inheritDoc
     */
    getHitTestInfo(xHitCoord, yHitCoord, nearestPointIndex, hitTestRadius, interpolate) {
        const hitTestInfo = this.createNewHitTestInfo(xHitCoord, yHitCoord, nearestPointIndex, hitTestRadius, interpolate);
        const bubbleSeries = this.parentSeries;
        // find distance from hit point to nearest data point
        const dx = xHitCoord - hitTestInfo.xCoord;
        const dy = yHitCoord - hitTestInfo.yCoord;
        const distanceToCenter = Math.sqrt(dx * dx + dy * dy);
        const nearestRadius = bubbleSeries.getBubbleDiameter(nearestPointIndex) / 2;
        hitTestInfo.isHit = distanceToCenter <= Math.max(nearestRadius, hitTestRadius);
        return hitTestInfo;
    }
}
exports.BubbleSeriesHitTestProvider = BubbleSeriesHitTestProvider;
