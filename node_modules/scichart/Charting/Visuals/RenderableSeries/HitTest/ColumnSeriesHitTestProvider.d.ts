import { FastColumnRenderableSeries } from "../FastColumnRenderableSeries";
import { BaseHitTestProvider } from "./BaseHitTestProvider";
import { HitTestInfo } from "./HitTestInfo";
/**
 * Hit-test provider for {@link FastColumnRenderableSeries}. See base class {@link BaseHitTestProvider} for further info
 */
export declare class ColumnSeriesHitTestProvider extends BaseHitTestProvider<FastColumnRenderableSeries> {
    /**
     * @inheritDoc
     */
    getNearestPoint2D(xHitCoord: number, yHitCoord: number): number;
    /**
     * @inheritDoc
     */
    protected getHitTestInfo(xHitCoord: number, yHitCoord: number, nearestPointIndex: number, hitTestRadius: number, interpolate: boolean): HitTestInfo;
}
