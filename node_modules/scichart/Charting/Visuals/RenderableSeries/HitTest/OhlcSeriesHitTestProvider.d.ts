import { FastOhlcRenderableSeries } from "../FastOhlcRenderableSeries";
import { BaseHitTestProvider } from "./BaseHitTestProvider";
import { HitTestInfo } from "./HitTestInfo";
/**
 * Hit-test provider for {@link FastOhlcRenderableSeries}. See base class {@link BaseHitTestProvider} for further info
 */
export declare class OhlcSeriesHitTestProvider extends BaseHitTestProvider<FastOhlcRenderableSeries> {
    /**
     * @inheritDoc
     */
    protected getHitTestInfo(xHitCoord: number, yHitCoord: number, nearestPointIndex: number, hitTestRadius: number, interpolate: boolean): HitTestInfo;
}
