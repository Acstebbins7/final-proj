import { IDeletable } from "../../../Core/IDeletable";
/**
 * Used by the {@link RubberBandXyZoomModifier} to draw an {@link SVGSVGElement | SVGElement} rectangle over the chart.
 */
export declare class RubberBandRect implements IDeletable {
    private svgRoot;
    private svg;
    private x1Property;
    private x2Property;
    private y1Property;
    private y2Property;
    private isHiddenProperty;
    constructor(svgRoot: SVGSVGElement, fill: string, stroke: string, strokeThickness: number);
    get x1(): number;
    set x1(value: number);
    get x2(): number;
    set x2(value: number);
    get y1(): number;
    set y1(value: number);
    get y2(): number;
    set y2(value: number);
    get isHidden(): boolean;
    set isHidden(value: boolean);
    delete(): void;
    private create;
    private update;
    private notifyPropertyChanged;
}
