import { ENumericFormat } from "../../../../types/NumericFormat";
import { LabelProvider } from "./LabelProvider";
/**
 * The {@link NumericLabelProvider} formats Axis Labels and Cursor / Tooltips for {@link NumericAxis} types
 */
export declare class NumericLabelProvider extends LabelProvider {
    numericFormat: ENumericFormat;
    /**
     * Creates an instance of {@link NumericLabelProvider}
     * @param format The desired {@link ENumericFormat | Label Format} enum
     */
    constructor(format?: ENumericFormat);
    /**
     * @inheritDoc
     */
    onBeginAxisDraw(): void;
    /**
     * @inheritDoc
     */
    formatCursorLabel(dataValue: number, formatString: string): string;
    /**
     * @inheritDoc
     */
    formatLabel(dataValue: number): string;
}
