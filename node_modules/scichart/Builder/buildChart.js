"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildChart = void 0;
const buildAxes_1 = require("./buildAxes");
const buildSeries_1 = require("./buildSeries");
exports.buildChart = (webAssemblyContext, scs, chartOptions) => {
    const { series, axes } = chartOptions;
    // draw axis
    buildAxes_1.buildAxes(webAssemblyContext, scs, axes);
    // draw series
    series.forEach(seriesOptions => {
        const lineSeries = buildSeries_1.buildSeries(webAssemblyContext, seriesOptions);
        if (lineSeries) {
            scs.renderableSeries.add(lineSeries);
        }
    });
};
