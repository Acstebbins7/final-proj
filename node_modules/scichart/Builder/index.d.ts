import { SciChartSurface } from "../Charting/Visuals/SciChartSurface";
import { ESeriesType } from "../types/SeriesType";
declare type TPoint2D = [number, number];
declare type TFastLineRenderableSeriesOptions = {
    color: string;
};
export declare type TSeriesOptions = {
    type: ESeriesType;
    fastLineRenderableSeriesOptions?: TFastLineRenderableSeriesOptions;
    thickness?: number;
    data: TPoint2D[];
};
export declare type TAxisOptions = {
    isVisible?: boolean;
    colorMajor?: string;
    colorMinor?: string;
    thickness?: number;
    tickSize?: number;
    strokeDasharray?: number[];
};
export declare type TChartOptions = {
    series: TSeriesOptions[];
    axes: TAxisOptions;
};
export declare const sciChartVanillaJS: {
    new: (canvasElementId: string, chartOptions: TChartOptions, width?: number, height?: number) => Promise<SciChartSurface>;
};
export {};
